id: docker-desktop-data
name: Docker Desktop local data root
category: docker
platform: windows
paths:
  - "%LOCALAPPDATA%\\Docker"
risk: review
cleanup:
  method: report-only
exclusions: []
reason: "Docker Desktop local data can include diagnostics and client-side cache, but should still be reviewed before cleanup."
warnings:
  - "Use docker-native commands for cleanup instead of file deletion."
