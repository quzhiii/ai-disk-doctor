id: wsl-ext4-vhdx
name: WSL ext4 virtual disk
category: wsl
platform: windows
paths:
  - "%LOCALAPPDATA%\\Packages\\*\\LocalState\\ext4.vhdx"
risk: system
cleanup:
  method: guide
exclusions: []
reason: "WSL ext4 virtual disks should be reported precisely and handled via guided compaction or relocation steps, never blind deletion."
warnings:
  - "Do not delete ext4.vhdx directly. Use WSL export, compact, or relocation workflows."
