# Category Map

主要分类与建议使用方式：

- `docker`: `doctor --docker`
- `wsl`: `doctor --wsl`
- `models`: `doctor --ollama` 或 `doctor --huggingface`
- `browser` / `browser-cache`: `scan --category browser-cache`
- `ai-agent`: `scan --category ai-agent`
- `sync-drive`: 先解释风险，不建议直接清理
