# Fixtures

These fixtures simulate realistic Windows AI development storage layouts.

Covered scenarios:

- WSL `ext4.vhdx`
- Docker `build-cache` and `docker_data.vhdx`
- Playwright `.playwright-browsers`
- Hugging Face cache
- Ollama model store
